{
  "name": "vms-api",
  "version": "0.0.1",
  "description": "self learn api project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run generate-spec && npm run generate-routes && ts-node-dev --respawn --transpile-only src/app.ts",
    "generate-routes": "npx tsoa routes",
    "generate-spec": "npx tsoa spec",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "swagger-ui-express": "^5.0.1",
    "tsoa": "^6.5.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.8",
    "@types/swagger-ui-express": "^4.1.6",
    "prisma": "^5.21.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}